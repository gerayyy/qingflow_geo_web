# Database
# 数据库连接URL，格式：mysql://用户名:密码@主机:端口/数据库名
# 用于连接外部MySQL数据库
DATABASE_URL="mysql://he:66@192.168.0.1:3306/ku"

# API
# API密钥，用于保护API端点，至少32个字符
API_SECRET_KEY="hd_Wq9ZiChWPIdyO"

# Environment
# 运行环境：development（开发）、production（生产）、test（测试）
NODE_ENV="production"

# Database Pool (Optional)
# 数据库连接池配置（可选）
# 最小连接数
DATABASE_POOL_MIN=2
# 最大连接数
DATABASE_POOL_MAX=10

# ISR (Optional)
# 增量静态再生配置（可选）
# 用于在生产环境中重新验证静态页面的密钥
REVALIDATE_SECRET=""

# Third Party Services (Optional)
# 第三方服务配置（可选）
# Sentry错误监控DSN
SENTRY_DSN=""
# 日志服务端点URL
LOG_SERVICE_ENDPOINT=""
# CDN缓存清除令牌
CDN_PURGE_TOKEN=""